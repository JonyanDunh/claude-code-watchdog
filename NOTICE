Watchdog
Copyright 2026 Jonyan Dunh

This product is licensed under the Apache License, Version 2.0 (the
"License"). You may not use this product except in compliance with the
License. You may obtain a copy of the License at:

    http://www.apache.org/licenses/LICENSE-2.0

================================================================================
Attribution
================================================================================

This product contains software derived from the "ralph-loop" plugin
developed by Anthropic, PBC and distributed under the Apache License,
Version 2.0:

    https://github.com/anthropics/claude-plugins-official/tree/main/plugins/ralph-loop

The original ralph-loop plugin is Copyright Anthropic, PBC, licensed under
Apache License, Version 2.0. The Watchdog plugin keeps ralph-loop's core
mechanic (a Stop hook that re-feeds the user's prompt to drive a
self-referential iterative loop inside a single Claude Code session) and
introduces the following substantive modifications:

  - Replaced the <promise> XML-tag exit protocol with a headless
    Claude Haiku classifier that reads the current turn's tool_use
    invocations and decides whether any files were modified. This
    catches Bash-driven file mutations (sed -i, mv, > file, git add,
    etc.) that a tool-name whitelist would miss.

  - Added an exit precondition: the turn must have called at least
    one tool before the loop is allowed to exit. A pure-text response
    ("I've verified, all good") never terminates the loop, preventing
    the agent from claiming completion without doing real verification.

  - Hid the loop from the agent. The Stop hook emits no systemMessage,
    the setup script writes only the user's prompt to stdout, and all
    diagnostic output is routed to stderr. The agent has no way to
    tell it is running inside a loop and therefore cannot short-circuit
    by emitting a fake completion signal.

  - Introduced per-session state files keyed by TERM_SESSION_ID so
    concurrent watchdogs in different terminal tabs do not clobber
    each other's state. The state file format was also switched from
    markdown-with-YAML-frontmatter to JSON.

  - Appended an English verification reminder to every re-fed prompt,
    instructing the agent to re-run the verification with real tool
    calls instead of answering from prior context or memory.

  - Fixed a transcript-parsing bug in the Stop hook where tool_result
    entries (which carry role=user in the JSONL transcript) were being
    mistaken for turn boundaries, causing the current turn's tool_use
    blocks to be excluded from the classification prompt.

  - Added an owner_session_id recursion guard. The headless Haiku
    classifier we spawn is itself a Claude Code session, so its own
    Stop hook fires when it ends. TERM_SESSION_ID is inherited by
    subprocesses, so the recursive invocation would find and clobber
    the main session's state file. The fix records the per-invocation
    Claude session_id (from the hook's stdin JSON, which is NOT
    inherited) as owner_session_id on first fire, and recursive fires
    with a different session_id bail out without touching state.

  - Rewrote the entire plugin from Bash + jq + POSIX coreutils to
    Node.js (v1.1.0). The new implementation has zero shell or POSIX
    dependencies — it runs natively on Linux, macOS, and native
    Windows (PowerShell / cmd) with only `node` 18+ required. Shared
    logic is extracted into reusable modules under lib/ (constants,
    state, transcript, judge, log, stdin), consumed by three thin
    entry points (setup-watchdog.js, stop-watchdog.js, stop-hook.js).
    The port is covered by an automated test suite using Node's
    built-in node:test runner (no external test dependencies).

  - Replaced TERM_SESSION_ID with a process-ancestry walk to find the
    parent Claude Code process ID (v1.2.0). Most terminal emulators
    (Windows Terminal, macOS Terminal.app, GNOME Terminal, Alacritty,
    tmux/screen, plain Linux ttys) do NOT export TERM_SESSION_ID —
    only JetBrains IDE terminals, iTerm2, and WezTerm do — so the
    v1.1.0 plugin was unusable for the majority of users. v1.2.0
    walks the process tree upward from its own ppid until it finds a
    process whose name is `claude`, and uses that PID as the
    per-session state file key. This also eliminates the need for
    the owner_session_id recursion guard: the headless Haiku
    classifier subprocess naturally gets its own fresh Claude Code
    process with a fresh PID, and its recursive Stop hook walks up
    to find THAT PID (not the main session's), so state files are
    naturally isolated without any explicit ownership bookkeeping.

Files significantly modified from their ralph-loop originals include:

  - hooks/stop-hook.js        (was hooks/stop-hook.sh in ralph-loop
                               and Watchdog 1.0.x)
  - scripts/setup-watchdog.js (originally scripts/setup-ralph-loop.sh)
  - scripts/stop-watchdog.js  (originally scripts/cancel-ralph.sh)
  - commands/start.md         (originally commands/ralph-loop.md)
  - commands/stop.md          (originally commands/cancel-ralph.md)
  - lib/constants.js          (new in Watchdog 1.1.0)
  - lib/state.js              (new in Watchdog 1.1.0)
  - lib/transcript.js         (new in Watchdog 1.1.0)
  - lib/judge.js              (new in Watchdog 1.1.0)
  - lib/log.js                (new in Watchdog 1.1.0)
  - lib/stdin.js              (new in Watchdog 1.1.0)
  - lib/claude-pid.js         (new in Watchdog 1.2.0)

See each file's header comment for a per-file attribution notice.
